name: fincalc_pro
description: Finora – FD, RD, SIP & EMI Calculator.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  get_it: ^7.6.4
  dio: ^5.3.3
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  fl_chart: 0.65.0
  intl: ^0.19.0
  go_router: ^12.1.1
  pdf: ^3.10.7
  csv: ^5.1.1
  path_provider: ^2.1.1
  share_plus: ^12.0.2
  flutter_animate: ^4.3.0
  shimmer: ^3.0.0
  url_launcher: ^6.2.1

  # NOTE: Enable these after Firebase setup:
  # firebase_core: ^2.24.2
  # firebase_auth: ^4.15.3
  # google_sign_in: ^6.1.6
  # cloud_firestore: ^4.13.6
  # firebase_messaging: ^14.7.10
  # google_mobile_ads: ^4.0.0
  # flutter_secure_storage: ^9.0.0
  # cached_network_image: ^3.3.0
  # flutter_svg: ^2.0.9
  # animations: ^2.0.8
  # encrypt: ^5.0.3
  # lottie: ^2.7.0
  # package_info_plus: ^5.0.1
  # connectivity_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/animations/

