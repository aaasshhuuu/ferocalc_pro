// Encryption service — stub for initial run
// Enable full AES-256 encryption after adding 'encrypt' package

class EncryptionService {
  String encrypt(String data) => data;
  String decrypt(String data) => data;
}
